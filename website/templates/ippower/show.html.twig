{% extends 'base.html.twig' %}

{% block title %}Ippower{% endblock %}

{% block body %}
    <h1>Ippower</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ippower.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ippower.name }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ ippower.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ippower_index') }}">back to list</a>

    <a href="{{ path('app_ippower_edit', {'id': ippower.id}) }}">edit</a>

    {{ include('ippower/_delete_form.html.twig') }}
{% endblock %}
