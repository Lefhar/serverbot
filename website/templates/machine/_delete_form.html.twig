<form method="post" action="{{ path('app_machine_delete', {'id': machine.id}) }}" onsubmit="return confirm('être vous sûr de vouloir supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ machine.id) }}">
    <div class="d-grid gap-2 pt-2">
        <button class="btn  btn-danger ">Effacer</button>
    </div>
</form>
