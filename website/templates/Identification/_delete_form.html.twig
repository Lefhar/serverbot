<form method="post" action="{{ path('app_Identification_delete', {'id': Identification.id}) }}" onsubmit="return confirm('être vous sûr de vouloir supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ Identification.id) }}">
    <div class="d-grid gap-2 pt-2">
        <button class="btn  btn-danger ">Effacer</button>
    </div>
</form>
